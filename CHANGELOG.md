# Changelog

## 2025-11-24
- Fix watchlist analytics logging (import resolved).
- Move query audit logging after summary so matching counts are recorded.
- Improve navigation: anchor-based top nav links, always-on sidebar reopen helper button.
- Clean landing CTA styles (remove unsupported selectors).
- Add multi-value column hints in schema mapper preview.
- Correct Dockerfile CMD for Social AE API (uvicorn with api workdir).
