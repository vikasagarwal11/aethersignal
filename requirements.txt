# Pinned versions known to work on Streamlit Cloud
# Note: Python 3.12 recommended (see runtime.txt) - scipy has pre-built wheels for 3.12
streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4
# scipy - no version pin to allow pip to find Python 3.13 compatible pre-built wheel
scipy
plotly==5.22.0
openpyxl==3.1.5
fpdf2==2.8.5
pennylane==0.38.0
pdfplumber==0.11.4
requests==2.31.0
supabase==2.0.0
python-dotenv>=1.0.0
transformers==4.35.0
# torch is optional - ML features will work without it (rule-based fallback)
# Uncomment below for local development with ML features:
# torch==2.1.0
# For Streamlit Cloud, install via: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Enhanced fuzzy matching (better performance than fuzzywuzzy, MIT licensed)
rapidfuzz==3.5.2
# AI/LLM dependencies (optional - install if using AI features)
openai>=1.0.0
anthropic>=0.18.0
huggingface_hub>=0.20.0  # For BioGPT via Hugging Face Inference API
# groq>=0.4.0  # Uncomment if using Groq API (for fast inference, not BioGPT)
# Enhanced cross-source deduplication (optional - ML-based duplicate detection)
recordlinkage>=0.16.0
